node_modules/
.DS_Store

# Environment & sensitive files
credentials.json
tokens.json
.env
*.env
key.env

# Scratch / stray files (not source)
.design-preview/
# Finder/editor duplicate-copy artifacts ("foo 2.ts")
**/* 2.*

# Runtime data — Postgres is the source of truth; these dirs are
# recreated by the server at startup (mkdirSync) and hold local
# uploads / legacy JSON, not source.
angular+socket/socket-server/projects/local/
angular+socket/socket-server/projects/hosted/
angular+socket/socket-server/users/
angular+socket/socket-server/src/users/
